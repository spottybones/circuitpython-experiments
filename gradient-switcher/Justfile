BOARD_PATH := / "Volumes" / "CIRCUITPY"
PWD := invocation_directory()

set fallback := true

@push:
    echo Pushing code to board...
    mkdir -p {{ BOARD_PATH }}/my_lib
    cp {{ PWD }}/*.py {{ BOARD_PATH }}/
    cp {{ PWD }}/my_lib/*.py {{ BOARD_PATH }}/my_lib/
